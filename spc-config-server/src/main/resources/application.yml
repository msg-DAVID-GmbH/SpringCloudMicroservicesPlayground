


# Defines the port where the config server is running so that rest of the services can pick up their external configurations
server:
  port: 8888

logging:
  level:
    org.springframework.cloud: 'DEBUG'
  pattern:
    console: "%d{HH:mm:ss} %clr(%.-1level) %msg%n"
