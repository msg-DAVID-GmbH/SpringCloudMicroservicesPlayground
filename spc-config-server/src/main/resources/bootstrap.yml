spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/martinfoersterling/spring-cloud-microservices-config
# While in development mode, you can configure the config server to pick up configuration files from the file system
# uri: file://
